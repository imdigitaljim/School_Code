Member 1: Jim Bach
Member 2: Matthew Harrison
Member 3: Robert Tuttle

p1-Bach-Harrison-Tuttle.tar contents:
	README
	report.txt
	main.c		//main implementation
	shellops.c	//shell definitions
	shellops.h	//shell declarations/interface
	makefile

Completed using: linprog

To run:
$> make run

To clean:
$> make clean

Known bugs:

To do:

Additional comments:
Any time dynamic allocation of arrays was required, we
allocated MAXLENGTH elements (512).
